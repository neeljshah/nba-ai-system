---
phase: 01-cv-pipeline-storage
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - tracking/seed_historical.py
  - data/seeds/seed_games.sql
  - data/seeds/seed_players.sql
autonomous: true
requirements:
  - DB-02

must_haves:
  truths:
    - "The games table contains at least two full NBA seasons worth of game records (sufficient historical depth for ML training)"
    - "The players table contains representative player records covering those game seasons"
    - "The seed script is idempotent — running it twice does not create duplicate records"
    - "The seed script documents clearly that real historical tracking_coordinates data must be loaded separately from actual processed video footage"
  artifacts:
    - path: "tracking/seed_historical.py"
      provides: "Historical seed script for games and players tables"
      exports: ["seed_historical"]
    - path: "data/seeds/seed_games.sql"
      provides: "INSERT statements for historical game records (2022-23 and 2023-24 NBA seasons)"
      contains: "INSERT INTO games"
    - path: "data/seeds/seed_players.sql"
      provides: "INSERT statements for player records"
      contains: "INSERT INTO players"
  key_links:
    - from: "tracking/seed_historical.py"
      to: "tracking/database.py"
      via: "imports get_connection() to execute SQL seed files"
      pattern: "get_connection"
    - from: "data/seeds/seed_games.sql"
      to: "games table"
      via: "INSERT INTO games with ON CONFLICT DO NOTHING for idempotency"
      pattern: "ON CONFLICT DO NOTHING"
---

<objective>
Seed the PostgreSQL database with enough historical game and player records that the ML training pipeline (Phase 3) has meaningful metadata to join against tracking coordinates.

Purpose: DB-02 requires "historical datasets suitable for ML training." The tracking_coordinates rows created during actual video processing (plan 01-04) need game and player FK references to exist. This plan creates those reference rows so Phase 1 ends with a data-ready database, not an empty schema.
Output: seed_games.sql (representative NBA season records), seed_players.sql (player roster records), seed_historical.py (runner script).
</objective>

<execution_context>
@C:/Users/neelj/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/neelj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-cv-pipeline-storage/01-01-SUMMARY.md
</context>

<interfaces>
From tracking/database.py (plan 01-01):
```python
def get_connection() -> psycopg2.connection: ...
def init_schema() -> None: ...
```

From tracking/schema.sql (plan 01-01):
```sql
-- games: id UUID, game_date DATE, home_team TEXT, away_team TEXT, season TEXT, created_at TIMESTAMPTZ
-- players: id UUID, name TEXT, team TEXT, jersey_number INT, position TEXT, created_at TIMESTAMPTZ
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Historical game and player seed SQL files</name>
  <files>data/seeds/seed_games.sql, data/seeds/seed_players.sql, data/__init__.py, data/seeds/__init__.py</files>
  <action>
    Create data/seeds/seed_games.sql with INSERT INTO games (id, game_date, home_team, away_team, season) VALUES ... ON CONFLICT (id) DO NOTHING.

    Include at minimum 40 games covering two seasons: 2022-23 and 2023-24. Use representative NBA teams: LAL, GSW, BOS, MIL, PHX, DEN, MIA, PHI, NYK, CLE, SAC, MIN, OKC, MEM, DAL, NOP.
    Use hardcoded UUIDs (gen_random_uuid() won't work in plain SQL inserts — use actual UUID strings like 'a1b2c3d4-e5f6-7890-abcd-ef1234567890' with sequential-ish patterns).

    Create data/seeds/seed_players.sql with INSERT INTO players (id, name, team, jersey_number, position) VALUES ... ON CONFLICT (id) DO NOTHING.

    Include at minimum 30 players covering key NBA stars across those teams: representative names, jersey numbers, and positions (PG, SG, SF, PF, C). Use consistent hardcoded UUIDs.

    Create data/__init__.py and data/seeds/__init__.py as empty files.

    NOTE: These records provide FK targets for tracking_coordinates. The actual coordinates (frame-by-frame tracking data) are populated by running the video pipeline on real footage — that is plan 01-04's job. This seed only provides the game/player reference dimension tables.
  </action>
  <verify>
    <automated>python -c "import pathlib; sql = pathlib.Path('data/seeds/seed_games.sql').read_text(); count = sql.count('INSERT INTO games'); assert count >= 1, 'No game inserts'; lines = [l for l in sql.split('\n') if 'VALUES' in l or \"'202\" in l]; print(f'Game seed file OK, ~{sql.count(chr(10))} lines')"</automated>
  </verify>
  <done>seed_games.sql has game records for 2022-23 and 2023-24 seasons with ON CONFLICT DO NOTHING. seed_players.sql has 30+ player records with ON CONFLICT DO NOTHING. Both use hardcoded UUID strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed runner script</name>
  <files>tracking/seed_historical.py</files>
  <behavior>
    - seed_historical() function connects to DB, executes seed_games.sql, executes seed_players.sql, commits, prints counts of rows in games and players tables after seeding
    - Running the script twice produces the same result (idempotent via ON CONFLICT DO NOTHING)
    - Script prints clear message: "NOTE: tracking_coordinates must be populated by running the video pipeline on actual game footage"
    - if __name__ == "__main__": calls seed_historical()
    - Script handles missing DATABASE_URL gracefully (prints connection error and exits 1, does not raise unhandled exception)
  </behavior>
  <action>
    Create tracking/seed_historical.py:
      - Import get_connection from tracking.database
      - seed_historical() function:
        1. Get connection via get_connection()
        2. Use pathlib.Path(__file__).parent.parent / "data/seeds/seed_games.sql" to find seed files
        3. Execute seed_games.sql content via cursor.execute()
        4. Execute seed_players.sql content via cursor.execute()
        5. Commit
        6. SELECT COUNT(*) from games and players, print results
        7. Print the NOTE about tracking_coordinates
      - Wrap in try/except EnvironmentError to handle missing DATABASE_URL cleanly
      - if __name__ == "__main__": calls seed_historical()

    The script is designed to be run once after the database is initialized (python -m tracking.seed_historical).
  </action>
  <verify>
    <automated>python -c "from tracking.seed_historical import seed_historical; import inspect; src = inspect.getsource(seed_historical); assert 'seed_games.sql' in src and 'seed_players.sql' in src; assert 'tracking_coordinates' in src; print('seed_historical function structure OK')"</automated>
  </verify>
  <done>seed_historical() function is importable, references both SQL seed files, prints the tracking_coordinates note, and handles missing DATABASE_URL without crashing. Running python -m tracking.seed_historical would seed the DB if DATABASE_URL is set.</done>
</task>

</tasks>

<verification>
python -c "from tracking.seed_historical import seed_historical; print('Seed module imports OK')"
python -c "import pathlib; games_sql = pathlib.Path('data/seeds/seed_games.sql').read_text(); players_sql = pathlib.Path('data/seeds/seed_players.sql').read_text(); assert 'ON CONFLICT' in games_sql and 'ON CONFLICT' in players_sql; print('Idempotency check OK')"
</verification>

<success_criteria>
- data/seeds/seed_games.sql has game records for 2022-23 and 2023-24 seasons using ON CONFLICT DO NOTHING
- data/seeds/seed_players.sql has 30+ player records using ON CONFLICT DO NOTHING
- tracking/seed_historical.py imports without error and references both SQL files
- seed_historical() is idempotent (safe to run multiple times)
- Script prints informational note about tracking_coordinates population requiring real video footage
</success_criteria>

<output>
After completion, create `.planning/phases/01-cv-pipeline-storage/01-03-SUMMARY.md`
</output>
