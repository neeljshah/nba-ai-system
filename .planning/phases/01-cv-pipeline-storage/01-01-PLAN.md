---
phase: 01-cv-pipeline-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tracking/__init__.py
  - tracking/database.py
  - tracking/schema.sql
  - pipelines/__init__.py
  - requirements.txt
  - .env.example
autonomous: true
requirements:
  - DB-01

must_haves:
  truths:
    - "The PostgreSQL database has all five tables: games, players, tracking_coordinates, possessions, shot_logs"
    - "requirements.txt declares all pipeline dependencies and the project is ready to install — any developer can run pip install -r requirements.txt to get a working environment"
    - "Downstream pipeline steps can connect to and initialize the database without additional configuration beyond setting DATABASE_URL"
  artifacts:
    - path: "tracking/schema.sql"
      provides: "DDL for all five tables with indexes and constraints"
      contains: "CREATE TABLE tracking_coordinates"
    - path: "tracking/database.py"
      provides: "PostgreSQL connection factory and schema initialization"
      exports: ["get_connection", "init_schema"]
    - path: "requirements.txt"
      provides: "Pinned dependencies for all pipeline components"
      contains: "ultralytics"
  key_links:
    - from: "tracking/database.py"
      to: "PostgreSQL"
      via: "psycopg2 connection using DATABASE_URL env var"
      pattern: "psycopg2\\.connect"
    - from: "tracking/schema.sql"
      to: "tracking/database.py"
      via: "init_schema() reads and executes schema.sql"
      pattern: "schema\\.sql"
---

<objective>
Bootstrap the project environment and create the PostgreSQL database schema that every subsequent pipeline step writes into.

Purpose: All CV and storage tasks depend on the database tables and Python packages existing. This plan creates those foundations before any parallel work begins.
Output: Five-table PostgreSQL schema, Python dependency manifest, project package structure, database connection module.
</objective>

<execution_context>
@C:/Users/neelj/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/neelj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project structure and Python dependencies</name>
  <files>requirements.txt, .env.example, tracking/__init__.py, pipelines/__init__.py, features/__init__.py, models/__init__.py</files>
  <behavior>
    - requirements.txt contains ultralytics>=8.0, deep-sort-realtime>=1.3, psycopg2-binary>=2.9, opencv-python>=4.8, torch>=2.0, numpy>=1.24, python-dotenv>=1.0
    - .env.example has DATABASE_URL=postgresql://user:password@localhost:5432/nba_tracking
    - All package directories exist with empty __init__.py files
    - pip install -r requirements.txt exits 0 (or packages already present)
  </behavior>
  <action>
    Create requirements.txt with these exact pinned versions (use >= not ==, to allow patch updates):
      ultralytics>=8.0
      deep-sort-realtime>=1.3
      psycopg2-binary>=2.9
      opencv-python>=4.8
      torch>=2.0
      numpy>=1.24
      python-dotenv>=1.0

    Create .env.example with:
      DATABASE_URL=postgresql://user:password@localhost:5432/nba_tracking
      VIDEO_INPUT_DIR=./data/videos
      MODEL_WEIGHTS_DIR=./data/models

    Create these empty __init__.py files to establish the package layout defined in PROJECT.md:
      tracking/__init__.py
      pipelines/__init__.py
      features/__init__.py
      models/__init__.py
      analytics/__init__.py
      api/__init__.py
      frontend/__init__.py
      dashboards/__init__.py

    Do NOT install packages yet — executor environment may differ. The verify step confirms the file exists and is readable.
  </action>
  <verify>
    <automated>python -c "import pathlib; lines = pathlib.Path('requirements.txt').read_text(); assert 'ultralytics' in lines and 'deep-sort-realtime' in lines and 'psycopg2-binary' in lines, 'Missing dependency'; print('requirements.txt OK')"</automated>
  </verify>
  <done>requirements.txt exists with all seven dependencies, .env.example exists, all eight package __init__.py files exist</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PostgreSQL schema and database module</name>
  <files>tracking/schema.sql, tracking/database.py</files>
  <behavior>
    - schema.sql creates all five tables without errors when run against a fresh PostgreSQL database
    - games table: id (UUID PK), game_date, home_team, away_team, season, created_at
    - players table: id (UUID PK), name, team, jersey_number, position, created_at
    - tracking_coordinates table: id (UUID PK), game_id (FK games), player_id (FK players nullable for ball), frame_number, timestamp_ms, x, y, velocity_x, velocity_y, speed, direction_degrees, object_type (player|ball), confidence, created_at. Index on (game_id, frame_number).
    - possessions table: id (UUID PK), game_id (FK), team, start_frame, end_frame, outcome, created_at
    - shot_logs table: id (UUID PK), game_id (FK), player_id (FK), frame_number, x, y, shot_type, made, defender_distance, shot_angle, created_at
    - database.py get_connection() returns psycopg2 connection using DATABASE_URL from env
    - database.py init_schema() reads schema.sql and executes it (uses IF NOT EXISTS — safe to run multiple times)
    - database.py raises clear error if DATABASE_URL not set
  </behavior>
  <action>
    Create tracking/schema.sql with CREATE TABLE IF NOT EXISTS for all five tables. Use UUID primary keys (gen_random_uuid() — requires pgcrypto extension, so include CREATE EXTENSION IF NOT EXISTS pgcrypto; at top). Add the composite index on tracking_coordinates(game_id, frame_number) for query performance.

    Create tracking/database.py:
      - Import psycopg2 and dotenv
      - load_dotenv() at module level
      - get_connection(): reads os.environ["DATABASE_URL"], returns psycopg2.connect(dsn). Raises EnvironmentError with clear message if DATABASE_URL is missing.
      - init_schema(): calls get_connection(), reads schema.sql relative to this file's directory (use pathlib.Path(__file__).parent), executes via cursor.execute(), commits, closes.
      - if __name__ == "__main__": calls init_schema() so it can be run directly as python -m tracking.database

    Use psycopg2-binary (not psycopg2) to avoid needing libpq headers on the host.
  </action>
  <verify>
    <automated>python -c "import pathlib, re; sql = pathlib.Path('tracking/schema.sql').read_text(); tables = ['games', 'players', 'tracking_coordinates', 'possessions', 'shot_logs']; missing = [t for t in tables if t not in sql]; assert not missing, f'Missing tables: {missing}'; print('Schema OK:', tables)"</automated>
  </verify>
  <done>tracking/schema.sql defines all five tables with FK relationships and index. tracking/database.py exports get_connection() and init_schema(). Running python -c "from tracking.database import get_connection, init_schema" imports without error.</done>
</task>

</tasks>

<verification>
python -c "from tracking.database import get_connection, init_schema; print('DB module imports OK')"
python -c "import pathlib; sql = pathlib.Path('tracking/schema.sql').read_text(); assert 'tracking_coordinates' in sql and 'possessions' in sql and 'shot_logs' in sql; print('All tables present')"
python -c "assert pathlib.Path('requirements.txt').exists() and pathlib.Path('.env.example').exists(); print('Config files OK')" 2>/dev/null || python -c "import pathlib; assert pathlib.Path('requirements.txt').exists(); print('Config files OK')"
</verification>

<success_criteria>
- requirements.txt contains all seven Python dependencies
- tracking/schema.sql contains CREATE TABLE IF NOT EXISTS for all five tables: games, players, tracking_coordinates, possessions, shot_logs
- tracking/database.py exports get_connection() and init_schema() without import errors
- All eight package directories exist with __init__.py
- .env.example documents required environment variables
</success_criteria>

<output>
After completion, create `.planning/phases/01-cv-pipeline-storage/01-01-SUMMARY.md`
</output>
